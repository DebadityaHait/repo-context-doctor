# Security policy

Repo Context Doctor is a local, read-only static analyzer. It does not make
network requests or execute instruction contents. Review reports before
sharing them outside your repository and never include secrets in issues.
